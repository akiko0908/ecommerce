@{
    Layout = "_LayoutAdmin";
    ViewData["TitleAdmin"] = "Quản lý Hệ điều hành SP";
}
@model IEnumerable<Ecommerce.Models.HeDieuHanh>

<div class="manage_title">
    <h3>Danh sách Hệ điều hành</h3>
</div>

@if (ViewBag.notifyMsg != null)
{
    <div id="nottifyMsg" class="alert alert-success" role="alert">
        @ViewBag.notifyMsg
    </div>
}

<script type="text/javascript">
    setTimeout(function () {
        $('#nottifyMsg').fadeOut('slow');
    }, 2500);
</script>

<hr>

<div style="margin: 0 auto; width: 40em;">
    <table class="table">
        <tr>
            <th>Mã Hệ điều hành</th>
            <th>Tên Hệ điều hành</th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.hdh_ID</td>
                <td>@item.hdh_Name</td>
                <td>
                    <a class="btn btn-info" asp-area="Admin" asp-controller="ManageCategories" asp-action="Edit"
                        asp-route-id="@item.hdh_ID">Edit</a> |
                    <a class="btn btn-danger" asp-area="Admin" asp-controller="ManageCategories" asp-action="Delete"
                        asp-route-id="@item.hdh_ID">Delete</a>
                </td>
            </tr>
        }
    </table>
</div>
